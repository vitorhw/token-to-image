"use client";

// Widget panel is no longer used as a standalone component.
// Widgets are now rendered as popovers directly from the prompt input tokens.
// This file is kept for backwards compatibility but the main logic moved to prompt-input.tsx

export {};
